cd ~/Vikram-GitHub

git init
git branch -M main

git add .

git status

git config --global user.name "Vikram Developer"
git config --global user.email "YOUR_GITHUB_EMAIL"

git commit -m "Vikram AI project"

git remote add origin https://github.com/USERNAME/REPOSITORY.git

git push -u origin maincd ~/Vikram-GitHub

git branch -M main

git config --global user.name "Vikram Developer"
git config --global user.email "YOUR_GITHUB_EMAIL"

git add .
git status

git commit -m "Vikram AI project"

git remote set-url origin https://github.com/YOUR_USERNAME/YOUR_REPOSITORY.git

git remote -v

git push -u origin main