cd ~/Vikram-GitHub

git init
git branch -M main

git add .

git status

git config --global user.name "Vikram Developer"
git config --global user.email "YOUR_GITHUB_EMAIL"

git commit -m "Vikram AI project"

git remote add origin https://github.com/USERNAME/REPOSITORY.git

git push -u origin main